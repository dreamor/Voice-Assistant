# MCP secrets — 不要提交到 git
#
# 在 mcp_servers.yaml 中通过 ${secrets.mcp.<server>.<key>} 引用。
# 复制为 config/secrets.yaml 并填入真实值。

mcp:
  github:
    token: ""
  slack:
    bot_token: ""

skills:
  weather:
    api_key: ""
